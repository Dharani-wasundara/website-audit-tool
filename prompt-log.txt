================================================================================
WEBAUDIT — PROMPT LOG (reference copy of all prompts used in the app)
================================================================================
This file is a static snapshot of the strings defined in code (lib/prompts.ts,
lib/audit-mock.ts). Runtime audits also return the same content via the API
promptLog object and the Results page “Prompt log” / Download JSON.

--------------------------------------------------------------------------------
1) GEMINI AUDIT — SYSTEM PROMPT (lib/prompts.ts — AUDIT_SYSTEM_PROMPT)
--------------------------------------------------------------------------------

You are a senior web performance analyst at a digital marketing agency 
specialising in SEO, conversion optimisation, content clarity, and UX.

You analyse single pages and return structured JSON reports. Your analysis 
is always:
- Grounded in the provided factual metrics (never invent data)
- Specific (cite exact numbers, not vague observations)
- Actionable (recommendations must be implementable)
- Prioritised by impact

You must return ONLY valid JSON matching the schema provided. 
No preamble, no markdown fences, no commentary outside the JSON.

--------------------------------------------------------------------------------
2) GEMINI AUDIT — USER PROMPT TEMPLATE (lib/prompts.ts — buildUserPrompt)
--------------------------------------------------------------------------------
Filled at runtime with: actual audited URL, JSON PageMetrics, and page markdown
(truncated to ~24,000 characters if longer; suffix “[...truncated for token budget]”).

## Audited URL
<runtime: url string>

## Factual Page Metrics
<runtime: JSON.stringify(metrics, null, 2)>

## Page Content (Markdown)
<runtime: Firecrawl markdown for the page, possibly truncated>

## Instructions
Analyse this page and return a JSON object with exactly this shape:

{
  "seo": {
    "score": <1-10>,
    "summary": "<2-3 sentence summary referencing specific metrics>",
    "issues": ["<specific issue with metric reference>", "..."]
  },
  "messaging": {
    "score": <1-10>,
    "summary": "...",
    "issues": [...]
  },
  "cta": {
    "score": <1-10>,
    "summary": "...",
    "issues": [...]
  },
  "ux": {
    "score": <1-10>,
    "summary": "...",
    "issues": [...]
  },
  "contentDepth": {
    "score": <1-10>,
    "summary": "...",
    "issues": [...]
  },
  "recommendations": [
    {
      "priority": <1-5>,
      "title": "<short action title>",
      "reasoning": "<why this matters, referencing a specific metric>",
      "metric_reference": "<the exact metric key and value this is based on>",
      "effort": "low|medium|high",
      "impact": "low|medium|high"
    }
  ]
}

Return exactly 3 to 5 recommendations (not fewer, not more), ordered by priority (1 = highest).
Every summary and reasoning field must cite at least one specific metric value.

--------------------------------------------------------------------------------
3) MOCK AUDIT MODE (no GEMINI_API_KEY or MOCK_AUDIT=true) — lib/audit-mock.ts
--------------------------------------------------------------------------------
When mock mode is used, no request is sent to Gemini. The stored “prompt” fields
in the response are placeholders; insights are generated deterministically from metrics.

System prompt (stored in promptLog.systemPrompt):
[MOCK MODE: no Gemini request was sent]

User prompt (stored in promptLog.userPrompt):
[MOCK MODE]
URL: <runtime url>

Metrics:
<runtime: JSON.stringify(metrics, null, 2)>

Raw model output (stored in promptLog.rawModelOutput):
<runtime: JSON string of the mock AuditInsights object, same schema as Gemini>

Model identifier: mock-local
Input tokens / output tokens: 0 / 0

Ensure every insight clearly connects at least one metric to a real-world impact (SEO, conversion, or UX).
Use the relationship between metrics and content to infer deeper insights (not just surface-level observations)
Avoid generic best practices unless directly justified by metrics
--------------------------------------------------------------------------------
END OF FILE
--------------------------------------------------------------------------------
